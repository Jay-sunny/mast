# 服务注册中心端口号server:  port: 8761spring:  application:    name: @artifactId@ # 服务注册中心名称  profiles:    active: dev # 服务注册中心多环境配置  security:    user:      name: mask # 服务注册中心密钥      password: mask  cloud:    config:      enabled: false